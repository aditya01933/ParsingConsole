<% unless @job.empty? %>
<%@job.each do |job|%>
	$('#job-id-container').prepend("<%= job.message %> </br></br>");
<%end%>
<%end%>
$("#last_time").text("<%= Time.now %>");
<% if @job.find_by(message: "done").present?%>
	$("#done").text("done");
<%end%>		